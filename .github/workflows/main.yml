name: setup KDE Plasma + CRD

on:
  workflow_dispatch:
    inputs:
      crd_full_command:
        description: 'Paste the FULL command string generated by the CRD headless setup page (including DISPLAY= and --code="...")'
        required: true
        type: string
